/** Primary app sections. */
export type SectionId = "notebook" | "inventory" | "settings";

/** Sections shown in the primary icon rail. */
export const VISIBLE_SECTIONS: SectionId[] = ["notebook", "inventory"];
