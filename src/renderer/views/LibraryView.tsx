import React from 'react';

/** Placeholder — replaced in commit 2 */
export function LibraryView() {
  return (
    <div className="flex flex-1 items-center justify-center">
      <span className="text-sm text-text-muted">Library</span>
    </div>
  );
}
